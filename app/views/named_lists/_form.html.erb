<%= form_for(named_list) do |f| %>
  <% if named_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(named_list.errors.count, "error") %> prohibited this named_list from being saved:</h2>

      <ul>
      <% named_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :list_name %>
    <%= f.text_field :list_name, {required: true} %>
  </div>

  <div class="field">
    <%= f.label :entry_name %>
    <%= f.text_field :entry_name, {required: true} %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
