<%= form_for(course_link) do |f| %>
  <% if course_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_link.errors.count, "error") %> prohibited this course_link from being saved:</h2>

      <ul>
      <% course_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.hidden_field :course_id %>
    <%= @course_link.course.title %>
  </div>

  <div class="field">
    <%= f.label :child_course_id %>
    <%= f.collection_select(:child_course_id, @courses, :id, :title, required: true) %>
  </div>

  <!--div class="field">
    <%= f.label :link_type %>
    <%= f.text_field :link_type %>
  </div-->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
